#!/usr/bin/node
// task 1 - Script with empty class Rectangle that defines a rectangle:
module.exports = class Rectangle {
  constructor (width, height) {
    this.width = width;
    this.height = height;
  }
};
