Doctest MyList class


``MyList(list)`` module class that inherits from list with
Public instance method: def print_sorted(self)

Import class

    >>> MyList = __import__('1-my_list').MyList

Test

    >>> my_list = MyList([3, 1, 2, 5, 4])
    >>> my_list.print_sorted()
        [1, 2, 3, 4, 5]

    >>> my_list = MyList([9, 4, 2, 7])
    >>> my_list.print_sorted()
        [2, 4, 7, 9]

    >>> my_list = MyList()

    >>> my_list
        []

    >>> my_list.append(1)

    >>> my_list.append(4)

    >>> my_list.append(3)

    >>> my_list
        [1, 4, 3]